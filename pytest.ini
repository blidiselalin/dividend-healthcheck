[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -q --tb=short
filterwarnings =
    ignore::DeprecationWarning
markers =
    integration: requires a populated vector database (skipped when data is sparse)
