# DividendScope - Core Dependencies
# Install with: pip install -r requirements.txt

# Web Framework (auth extra enables st.login / Google OIDC)
streamlit[auth]>=1.42.0

# Data & Analysis
pandas>=2.0.0
jinja2>=3.1.5
yfinance>=0.2.36

# Vector Database (local fallback when DATABASE_URL is unset — e.g. pytest)
chromadb>=0.4.22

# PostgreSQL (Docker Compose production; optional for local dev)
psycopg[binary]>=3.1.18
psycopg-pool>=3.2.0

# Visualization
plotly>=5.18.0

# PDF Reports (optional)
reportlab>=4.0.0

# Data Download (optional)
requests>=2.31.0
beautifulsoup4>=4.12.0

# Development (optional)
pytest>=8.0.0
pre_commit>=4.6.0
mypy>=1.8.0
pandas-stubs>=2.0.0
