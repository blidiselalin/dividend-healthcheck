# DividendScope - Core Dependencies
# Install with: pip install -r requirements.txt

# Web Framework (auth extra enables st.login / Google OIDC)
streamlit[auth]>=1.42.0

# Data & Analysis
pandas>=2.0.0
yfinance>=0.2.36

# Vector Database (local fallback when DATABASE_URL is unset)
chromadb>=0.4.22

# Cloud SQL PostgreSQL (optional — set DATABASE_URL to enable)
psycopg[binary]>=3.1.18
psycopg-pool>=3.2.0

# Visualization
plotly>=5.18.0

# PDF Reports (optional)
reportlab>=4.0.0

# Data Download (optional)
requests>=2.31.0
beautifulsoup4>=4.12.0

# Development (optional)
pytest>=8.0.0
# black>=24.0.0
# mypy>=1.8.0
